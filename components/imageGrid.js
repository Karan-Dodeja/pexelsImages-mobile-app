import { View, Text } from "react-native";

const ImageGrid = () => {
  return (
    <View>
      <Text>imageGrid</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ImageGrid;
